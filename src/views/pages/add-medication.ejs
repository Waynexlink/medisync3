<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-50">
    <div class="min-h-screen">
      <!-- Header -->
      <header class="bg-white shadow">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex justify-between h-16">
            <div class="flex items-center space-x-4">
              <a
                href="/dashboard"
                class="text-xl font-semibold text-gray-900 hover:text-gray-700"
                >MedSync</a
              >
              <span class="text-gray-400">|</span>
              <h1 class="text-lg text-gray-600">Add Medication</h1>
            </div>
            <div class="flex items-center space-x-4">
              <a
                href="/dashboard"
                class="text-sm text-gray-600 hover:text-gray-900"
                >Dashboard</a
              >
              <a href="/logout" class="text-sm text-red-600 hover:text-red-500"
                >Logout</a
              >
            </div>
          </div>
        </div>
      </header>

      <!-- Main Content -->
      <main class="max-w-2xl mx-auto py-6 sm:px-6 lg:px-8">
        <div class="px-4 py-6 sm:px-0">
          <div class="bg-white shadow rounded-lg">
            <div class="px-4 py-5 sm:p-6">
              <h2 class="text-xl font-semibold text-gray-900 mb-6">
                Add New Medication
              </h2>

              <!-- Success/Error Messages -->
              <% if (error) { %>
              <div
                class="mb-4 bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded"
              >
                <%= error %>
              </div>
              <% } %> <% if (success) { %>
              <div
                class="mb-4 bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded"
              >
                <%= success %>
              </div>
              <% } %>

              <!-- Form -->
              <form action="/add-medication" method="POST" class="space-y-6">
                <div>
                  <label
                    for="name"
                    class="block text-sm font-medium text-gray-700"
                    >Medication Name</label
                  >
                  <input
                    type="text"
                    id="name"
                    name="name"
                    required
                    class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-red-500 focus:border-red-500"
                    placeholder="e.g., Aspirin, Metformin"
                  />
                </div>

                <div>
                  <label
                    for="dosage"
                    class="block text-sm font-medium text-gray-700"
                    >Dosage & Instructions</label
                  >
                  <input
                    type="text"
                    id="dosage"
                    name="dosage"
                    class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-red-500 focus:border-red-500"
                    placeholder="e.g., 100mg, Take with food"
                  />
                </div>

                <div>
                  <label
                    for="schedule"
                    class="block text-sm font-medium text-gray-700"
                    >Schedule</label
                  >
                  <input
                    type="text"
                    id="schedule"
                    name="schedule"
                    required
                    class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-red-500 focus:border-red-500"
                    placeholder="e.g., Morning, Evening (separate with commas)"
                  />
                  <p class="mt-1 text-sm text-gray-500">
                    Enter times separated by commas (e.g., "8:00 AM, 6:00 PM" or
                    "Morning, Evening")
                  </p>
                </div>

                <div class="flex justify-between">
                  <a
                    href="/dashboard"
                    class="px-4 py-2 border border-gray-300 rounded-md text-sm font-medium text-gray-700 hover:bg-gray-50"
                  >
                    Cancel
                  </a>
                  <button
                    type="submit"
                    class="px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-red-600 hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500"
                  >
                    Add Medication
                  </button>
                </div>
              </form>
            </div>
          </div>
        </div>
      </main>
    </div>
  </body>
</html>
